export default {
  placeholder: {
    type: String,
    default: '关键字搜索'
  },
  //注意：如果是用了input[type=color]，则value必须是七位的#rrggbb
  background: { type: String, default: '#ffffff' },
  inputBackground: { type: String, default: '#f1f1f1' },
  icon: { type: String, default: '' }
}
